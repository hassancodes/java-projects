public class bittobin {
    public static int bitToBinFunc(String bits){
        decimalNum  = 0;
         for(int i=0; i<bits.length();i++){
             if(Integer.parseInt(String.ValueOf(charAtAt(i)))==1){

             }
         }
    }
}
